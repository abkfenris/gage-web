# Some packages are much faster installed by conda on travis.ci
# So we keep them seperate and let travis grab them from there instead

#numpy==1.9.2
matplotlib==1.4.3
#scipy==0.15.1
#pandas==0.16.2
seaborn==0.6.0
